name: Deploy Frontend

on:
  push:
    branches: [main]
  workflow_dispatch:

# ── Azure target resources (existing RG, new Container App) ────────────────────
env:
  IMAGE: dharshanscientist/mom-assist-frontend
  AZURE_RESOURCE_GROUP: rg_talent_search
  AZURE_CONTAINERAPP: mom-assist-frontend

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # VITE_API_BASE_URL is baked into the JS bundle at build time —
          # set it to the backend Container App URL
          # (e.g. https://mom-assist-backend.<region>.azurecontainerapps.io)
          build-args: |
            VITE_API_BASE_URL=${{ secrets.VITE_API_BASE_URL }}
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}

      - name: Login to Azure
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Deploy to Azure Container Apps
        run: |
          az containerapp update \
            --name ${{ env.AZURE_CONTAINERAPP }} \
            --resource-group ${{ env.AZURE_RESOURCE_GROUP }} \
            --image ${{ env.IMAGE }}:${{ github.sha }}
